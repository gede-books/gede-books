{
    "builds": [{
        "src": "gede_books/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "gede_books/wsgi.py"
        }
    ]
}